<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>Asteroids</RootNamespace>
    <AssemblyName>Asteroids</AssemblyName>
    <ApplicationIcon>App.ico</ApplicationIcon>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Raylib-cs" Version="*" />
    <PackageReference Include="System.Resources.Extensions" Version="10.0.5" />
  </ItemGroup>

  <ItemGroup>
    <Content Include="Sounds\**\*">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
  </ItemGroup>

  <!-- Exclude the vb-asteroids and Backup subdirectories from compilation -->
  <ItemGroup>
    <Compile Remove="vb-asteroids\**" />
    <Compile Remove="Backup\**" />
    <EmbeddedResource Remove="vb-asteroids\**" />
    <EmbeddedResource Remove="Backup\**" />
    <None Remove="vb-asteroids\**" />
    <None Remove="Backup\**" />
  </ItemGroup>

</Project>

